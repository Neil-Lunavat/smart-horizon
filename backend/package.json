{
  "name": "smart-horizon-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Express API for CSV-based model inference",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^2.0.2"
  }
}
